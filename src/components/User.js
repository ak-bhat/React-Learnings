const User = ({name, location}) => {
    return(
        <div className="user-card">
            <h3>Name:{name}</h3>
            <h3>Name:{location}</h3>
        </div>
    )
}

export default User;